# ===== 基础科学计算 / 工具 =====
numpy
scipy
numba
soundfile
librosa
anyascii
typing_extensions

# ===== Coqui TTS（新维护的 fork，不用老的 TTS 包）=====
coqui-tts
coqpit-config
coqui-tts[zh]
# ===== ASR：FunASR + ModelScope =====
funasr
modelscope
onnxruntime

# ===== 录音 / 播放（按需选一个）=====
# Windows 上：
pyaudio
# 或者跨平台：
sounddevice

# ===== 备用 TTS（系统自带声音，做 fallback 用，不想要可以删）=====
pyttsx3

# ===== LLM / HTTP 调用 =====
requests
openai

# ===== 语音增强相关 =====
noisereduce>=3.0.0        # 经典降噪库
webrtcvad>=2.0.10         # 语音活动检测
rnnoise-python>=1.0.0     # RNNoise降噪库
librosa>=0.10.0           # 音频处理 (已有)
scipy>=1.10.0             # 信号处理 (已有)

# ===== 声纹识别相关 =====
speechbrain>=0.5.16       # SpeechBrain框架 (ECAPA-TDNN)
torch>=2.0.0              # PyTorch
torchaudio>=2.0.0         # 音频处理
scikit-learn>=1.3.0       # 相似度计算
huggingface-hub>=0.17.0   # HuggingFace模型下载

# ===== 杂项（日志、配置等，一般 stdlib 就够，这里不额外加）=====
pandas
modelscope
tensorflow